# Codex CLI 环境变量
# 由 Ansible 模板渲染生成，请勿手动修改
# Codex CLI 启动时自动从 ~/.codex/.env 加载
{% set active = codex_model_configs[use_codex_model] %}
{% if active.env is defined %}
{% for key, value in active.env.items() %}
{{ key }}={{ value }}
{% endfor %}
{% endif %}
